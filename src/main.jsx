import React from "react";
import ReactDOM from "react-dom/client";
import ValuationApp from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ValuationApp />
  </React.StrictMode>
);
